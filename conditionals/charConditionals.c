#include <stdio.h>
#include <stdlib.h>

void main()
{
    char letter = 'x';

    if (letter == 'x') {
        printf("The letter is x");
    }

    printf("\nLetter's code on the ASCII table = %d", letter);
}